import Carousel from "./Carousel";

const Home = () => {
    return (
        
        <header className="masthead bg-primary text-white text-center" id="home">
            <Carousel />
        </header>
    );
};

export default Home;
